import { useState } from 'react';

interface MVT_AddSheetProps {
  pIdItem?: string;
}

export const MVT_AddSheet = ({ pIdItem }: MVT_AddSheetProps) => {
  //   const [_idTypeMovement, setIdTypeMovement] = useState('');
  //   const [_quantity, setQuantity] = useState(0);
  //   const [_idItem, setIdItem] = useState(pIdItem ? pIdItem : '');

  //   <AddSheet
  //     title="Faire un nouveau mouvement"
  //     queryKey={['movements']}
  //     mutationFn={() => createMovement(name)}
  //     onReset={() => setName('')}
  //     isSubmitDisabled={name.trim().length < 2}
  //   >
  //     <div className="grid gap-3">
  //       <Label htmlFor="name-movement-type">Nom</Label>
  //       <Input id="name-movement-type" onChange={(e) => setName(e.target.value)} value={name} />
  //     </div>
  //   </AddSheet>;
  return null;
};
